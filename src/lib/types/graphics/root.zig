pub const Vertex = @import("Vertex.zig");

pub const uniform_buffer = @import("uniform_buffer.zig");
pub const UniformBuffer = uniform_buffer.UniformBuffer;
pub const UniformBufferObject = uniform_buffer.UniformBufferObject;

pub const push_constants = @import("push_constants.zig");
pub const ObjectConstants = push_constants.ObjectConstants;

pub const Index = u16;
